@page
@model TalentedKidsCommunity.Pages.Kids.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kid[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kid[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kid[0].Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kid[0].EnrollmentDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Kid) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnrollmentDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.KidID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.KidID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.KidID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
